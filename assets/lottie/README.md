# lottie-web (offline)

Để chạy offline (trong exe), tải `lottie.min.js` về và đặt tên `lottie-web.min.js` tại đây:

CDN: https://cdnjs.cloudflare.com/ajax/libs/bodymovin/5.12.2/lottie.min.js

Nếu thiếu file này, app tự fallback tải lottie từ CDN (cần internet lần đầu).
