# PySide6 full: cần QtMultimedia (nhạc, nằm trong Addons). Render pet = QPixmap sprite,
# KHÔNG dùng QtWebEngine nữa -> build.ps1 --exclude-module strip WebEngine khỏi exe.
PySide6>=6.6
plyer>=2.1
pyinstaller>=6.0
